__pycache__/
*.py[cod]
.venv/
venv/
.idea/
.vscode/
.DS_Store

# Regenerable: the full scored test frame is 22 MB and is rebuilt by
# `python run_prototype.py`. The summary tables next to it are committed.
outputs/data/*.parquet
outputs/*.log

# Real feed data: downloaded on demand by `python -m railcast.feeds fetch`
data/raw/
data/cache/
# Collected live position reports are the user's own accumulated history
data/live/


outputs/pptx/

# deck files are built by hand and live outside the repo
*.pptx
